# ReservaClass (React + Vite + Tailwind + TypeScript)

## Scripts
```bash
npm i
npm run dev
```

## Rutas
/  (Splash)  
/login  
/register  
/home  
/categories  
/classes/:type  
/confirmation  

## Alias
Puedes importar con `@/` (config en `vite.config.ts` y `tsconfig.json`).
